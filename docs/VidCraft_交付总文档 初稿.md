# VidCraft 交付总文档

> 电商场景 AIGC 带货视频生成系统 · AI 全栈挑战赛
> 本文档为评审主入口，遵循「速览 → 价值 → 实现 → 可复核」动线，长技术文档以附录外链挂出。

---

## 0. 项目速览（30 秒读懂）

| 项 | 内容 |
|---|---|
| **项目名称 / 课题** | VidCraft · 电商场景 AIGC 带货视频生成系统 |
| **一句话核心价值** | 商家只需上传一张商品主图，即可端到端自动产出一条可直接发布的 TikTok 带货短视频，并沉淀「素材—剧本—创作」可复用资产。 |
| **在线 Demo** | https://vidcraft.icu （**首页「游客体验」免注册进入**，预置示例项目/剧本/视频） |
| **演示视频** | 〔待补：3-8 分钟录屏公开链接〕 |
| **源代码仓库** | 〔待补：GitHub 仓库链接〕 · 主分支 `master` · 185+ commits |
| **完成度** | **已部署可体验版**（线上 HTTP 200 运行中，超出 MVP，接近生产级） |
| **团队成员与分工** | 〔待补，建议格式见 §1〕 |

---

## 1. 团队与分工

| 成员 | 学校 / 专业 | 角色 | 负责模块 |
|---|---|---|---|
| 〔待补〕 | | 素材 + 剧本链路 | 素材库、向量检索、爆款拆解、剧本生成 |
| 〔待补〕 | | 创作 + 智能剪辑 | 一键成片、分镜编辑器、FFmpeg 合成、TTS/字幕 |
| 〔待补〕 | | 分发归因 + 数据看板 + 部署 | 数据看板、多因子归因、CI/CD、可观测性、上线运维 |

> 团队 2-3 人，建议按上表「三条链路」分工填写。

---

## 2. 核心功能清单

围绕官方「素材库建设 → 剧本生成 → 视频创作 → 数据回流」主链路，已实现 6 大能力：

1. **素材库（多颗粒度结构化）** — 商家上传商品主图 / 商品视频 / 参考素材，自动做多模态理解：提取商品维度（主体/类目）、视频维度（整体摘要 + Embedding）、切片（slice）维度细粒度特征，形成可被下游消费的结构化资产，支持关键词 / 标签 / 向量相似度检索。
2. **爆款视频拆解（优质视频库）** — 上传或导入站外爆款视频，自动生成结构化拆解报告（Hook 手法、卖点、分镜、风格、创意因子），只保存分析结果、不复刻原视频，沉淀「优质视频库」。
3. **方法论提炼（因子库 / 灵感模板）** — 对爆款拆解结果归一为「策略 + 因子」结构化模板，因子按 `CreativeFactors` 枚举对齐，作为剧本生成的可复用配方。
4. **剧本生成与干预** — 输入商品信息 + 策略/因子，由大模型组合出完整剧本（叙事框架、视觉风格、分镜脚本含画面/镜头运动/BGM/字幕、约束清单）；支持 Prompt 微调、分镜增删、台词改写、单分镜携因子重生成。
5. **一键成片 + 智能剪辑** — 端到端产出 ≤15s 竖版带货视频：文生图/图生视频生成分镜画面 → FFmpeg 拼接转场 → 多语种 TTS 配音 + 字幕烧录（libass）+ BGM 合成 → 多分辨率/画幅导出。
6. **任务进度与数据看板** — 长任务全程进度可视化（队列 + WebSocket 推送）、失败自动重试与兜底；数据看板以「生成因子 × 转化效果」做多因子归因可视化（mock 分发/转化数据）。

---

## 3. 端到端使用流程

1. 商家访问 https://vidcraft.icu，点击「游客体验」免注册进入工作台。
2. 在**素材库**上传商品主图（或商品链接/视频），系统调用 Doubao Vision 自动解析出商品主体、类目、卖点，并对视频素材切片、生成 Embedding 入向量库。
3. 进入**剧本工作室**：选择「爆款仿写 / 灵感模板 / 策略+因子组合」任一模式，输入商品信息，一键生成分镜剧本；可在分镜时间轴上微调台词、替换因子、增删分镜。
4. 确认剧本后点击**一键成片**，系统创建生成任务进入队列，文生图/图生视频逐分镜生成画面。
5. 页面通过进度条 + WebSocket 实时展示每个分镜状态；失败分镜自动重试（最多 2 次），整体不阻塞。
6. Worker（FFmpeg）拼接分镜、加转场、烧录字幕、合成 TTS 配音与 BGM，产出成片。
7. 在**预览页**在线播放，支持单分镜重渲染（不重渲整片）与多分辨率 / 9:16、16:9 画幅导出。
8. 在**数据看板**查看该视频生成因子与（mock）转化指标的归因分析，反哺下一轮创作。

> 📹 配套演示视频：〔待补〕

---

## 4. 系统架构

### 4.1 架构图

> 〔待补：架构图（建议在线画板导出 PNG 内嵌）。下方为文字框图，可直接据此绘制。〕

```
                          ┌─────────────────────────────────────┐
   商家浏览器 / 移动端  ──►│  Nginx (静态托管 + /api 反代 + 媒体代理) │
                          └───────────────┬─────────────────────┘
                                          │
        ┌─────────────────────────────────┼──────────────────────────────┐
        │                                  │                               │
 ┌──────▼───────┐               ┌──────────▼──────────┐          ┌─────────▼─────────┐
 │  Frontend    │   REST/WS     │     Backend         │  HTTP    │  Worker           │
 │ React19+Vite │◄────────────► │  NestJS 10          │ ───────► │  Python FastAPI   │
 │ AntD/Zustand │               │  REST + WebSocket   │          │  FFmpeg 合成流水线 │
 │ Recharts     │               │  BullMQ 任务编排     │          │  scene_detect     │
 └──────────────┘               └────┬─────────┬──────┘          └───────────────────┘
                                     │         │
                  ┌──────────────────┘         └───────────────────┐
                  │                                                 │
        ┌─────────▼──────────┐  ┌──────────────┐  ┌────────────────▼─────────────────┐
        │ PostgreSQL 16      │  │ Redis 7      │  │  火山引擎 OpenAPI                  │
        │ + pgvector(1024维) │  │ 队列/缓存    │  │  Doubao Vision/LLM · Seedance     │
        │ 13 张业务表        │  │              │  │  TTS(seed-tts-2.0) · Embedding    │
        └────────────────────┘  └──────────────┘  └───────────────────────────────────┘
        ┌────────────────────┐  ┌──────────────────────────┐
        │ MinIO 对象存储      │  │ OpenTelemetry → Jaeger    │
        │ (素材/成片/缩略图)  │  │ 全链路 trace              │
        └────────────────────┘  └──────────────────────────┘
```

### 4.2 核心技术栈

| 层级 | 技术选型 |
|---|---|
| 前端 | React 19 + TypeScript + Vite + Ant Design 5 + Zustand + React Router 7 + Recharts + socket.io-client（MSW 做 mock） |
| 后端 | NestJS 10 + TypeScript + TypeORM + BullMQ + Socket.IO + Passport/JWT + Swagger |
| 视频合成 | Python 3.12 + FastAPI + FFmpeg（libass 字幕）+ 场景检测 |
| 数据库 | PostgreSQL 16 + **pgvector**（向量检索，1024 维） |
| 缓存 / 队列 | Redis 7 + BullMQ（Bull Board 可视化） |
| 对象存储 | MinIO（S3 兼容） |
| AI 能力 | 火山引擎 OpenAPI：Doubao（Vision/LLM）、Seedance（文生/图生视频）、TTS、Embedding |
| 可观测性 | OpenTelemetry + Jaeger 全链路追踪 |
| 工程化 | ESLint + Prettier + StyleLint + Husky + lint-staged；GitHub Actions CI/CD |
| 部署 | Docker + Docker Compose（Nginx / Backend / Worker / PG / Redis / MinIO / Jaeger） |

### 4.3 大模型 / AI 能力使用说明

| 能力 | 模型 / API | 在系统中的位置 |
|---|---|---|
| 商品图理解 | Doubao Vision（ark `/chat/completions`） | 素材模块：主图解析出主体/类目/卖点 |
| 视频拆解 | Doubao Vision | 爆款拆解：抽帧 + 多模态理解生成结构化报告与创意因子 |
| 剧本生成 | Doubao LLM | 剧本模块：策略+因子+商品信息 → 分镜剧本 |
| 文生图 / 图生视频 | Seedance（支持 r2v 参考图，2.0） | 创作模块：逐分镜生成画面，含回调 `callback_url` |
| 语音合成 | 火山 TTS（`seed-tts-2.0`，多语种/多音色） | 创作模块：分镜配音（polyglot/dubbing） |
| 向量化 | 火山 Embedding（1024 维） | 素材检索：slice/视频 Embedding 入 pgvector，向量召回 |
| RAG / 检索 | pgvector 相似度 + 关键词/标签 | 剧本/创作的智能召回与爆款匹配 |
| Agent 编排 | BullMQ 多阶段任务流（解析→生成→合成→重试） | 长链路一键成片的任务编排与失败兜底 |

### 4.4 数据层（13 张业务表）

`users` · `projects` · `materials` · `material_slices` · `scripts` · `videos` · `video_tasks` · `video_metrics` · `viral_genes` · `viral_library` · `factor_definitions` · `diagnosis_reports` · `analyzed_videos`

> 完整字段与 ER 关系见附录《DDD 数据库设计文档》。

---

## 5. 关键工程难点与解决方案

1. **素材多颗粒度结构化 + 向量检索对齐**
   难点：图像/视频需在「商品 / 视频 / slice」三层提取特征，且 Embedding 维度必须与 pgvector 列严格一致，否则插入失败、召回全空。
   方案：统一走火山 Embedding ep 产出 **1024 维**向量，对齐 `materials.embedding vector(1024)` 列；视频按场景切片后逐 slice 向量化入库，检索时关键词 + 标签 + 向量相似度三路召回。

2. **长耗时视频任务的进度体验与失败兜底**
   难点：单条成片需多次调用文生图/图生视频/TTS，耗时长且任一分镜可能失败或超时。
   方案：BullMQ 队列编排多阶段任务 + Socket.IO 实时推送分镜级进度；Seedance 接入 `callback_url` 减少轮询；单分镜失败自动重试（`MAX_SHOT_RETRIES=2`），失败不阻塞整片，前端给出清晰失败反馈与重试入口。

3. **成片字幕 / 配音的合成可靠性**
   难点：FFmpeg 默认不带 libass，字幕烧录与多语种 TTS 配音常因环境缺失而静默失败。
   方案：Worker 镜像确保 FFmpeg 编译带 libass，默认字体 PingFang SC；TTS 走 `seed-tts-2.0` 多音色，配音 + 字幕 + BGM 在 FFmpeg 流水线统一合成；支持单分镜重渲染而非整片重渲。

4. **创意因子的中英归一与稳定存储**
   难点：AI 拆解输出为中文自然语言，直接存储无法稳定聚合与归因。
   方案：AI 出中文 → 后端幂等归一到 `CreativeFactors` 枚举码存储 → 展示层映射回中文，保证因子在「拆解—模板—剧本—归因」全链路一致可统计。

5. **从本地到生产的部署收敛（26 项问题排查）**
   难点：图片用 Docker 内网 hostname、媒体无持久卷、CORS、健康检查、日志膨胀等。
   方案：详见附录《部署问题诊断报告》，已修复 18/26 项（媒体公网地址、uploads 持久卷、CORS 白名单、entrypoint 自动迁移、全服务 health check、日志轮转、资源限额等）。

---

## 6. 部署与访问说明

- **线上访问**：https://vidcraft.icu —— 首页「游客体验」**免登录**直接体验核心链路，无权限申请、无付费墙。
- **部署形态**：Docker Compose 单机编排（Nginx + Backend + Worker + PostgreSQL + Redis + MinIO + Jaeger）。
- **CI/CD**：GitHub Actions，提交触发构建；生产 Dockerfile + `entrypoint.sh` 启动前自动执行 DB 初始化/迁移。
- **本地一键启动**：见附录 README（`npm run docker:up` → `npm run db:init && db:seed` → `npm run dev`）。
- **关键入口**：前端 `:5173`、API/Swagger `:3000/api`、Bull Board `:3000/admin/queues`、Jaeger `:16686`、MinIO `:9001`。

---

## 7. 项目亮点 / 创新点

1. **爆款 DNA 提取并复用** — 站外爆款视频拆解为「策略 + 因子」结构化配方，沉淀因子库并直接驱动剧本生成，形成「找参考 → 提炼方法论 → 生产剧本」闭环（合规：只存分析结果不复刻原视频）。
2. **多因子归因数据看板** — 将「生成因子 × 转化效果」可视化，让创作从凭感觉变为可归因、可迭代，回流反哺下一轮生成。
3. **真·端到端一键成片 + 分镜级干预** — 一张主图到成片全自动，同时保留分镜级重生成、因子替换、单镜重渲染的精细控制，兼顾「全自动」与「可控」。

---

## 8. 完成度对照（功能分级）

| 等级 | 要求 | 状态 |
|---|---|---|
| **P0 必做** | 素材上传 / 剧本生成 / 基础分镜 / 一键成片 / 任务进度 / 预览导出 | ✅ 全部完成 |
| **P1 进阶** | 素材标签&Embedding 检索 / 智能剪辑 / 分镜级编辑 / TTS&字幕&BGM / 失败重试 / 生成 trace / 数据看板 | ✅ 基本全部完成 |
| **P2 加分** | 多因子归因 / Agent 编排 / CI-CD / 可观测性 / 长任务体验 / 合规审核流 | ✅ 多数命中 |

---

## 9. 附录（可复核材料外链）

| 材料 | 说明 | 链接 |
|---|---|---|
| README / 运行说明 | 项目简介、依赖、启动步骤、目录结构 | `./README.md` |
| API 接口文档 | 全量接口规范；线上 Swagger `/api` | `./docs/API接口规范文档.md` |
| 数据库设计 / ER 图 | 13 张表字段与关系 | `./docs/DDD数据库设计文档.md` |
| 产品需求文档 | PRD | `./docs/PRD产品需求文档.md` |
| 技术设计文档 | TDD | `./docs/TDD技术设计文档.md` |
| 模块设计文档 | MDD | `./docs/MDD模块设计文档.md` |
| 部署问题诊断报告 | 26 项排查与修复记录 | `./docs/deploy-issues-report.md` |
| 产品截图集 | 关键页面截图 | 〔待补〕 |
| 演示视频 | 3-8 分钟 | 〔待补〕 |

---

> **待人工补充清单**：① 团队成员与分工（§1）② 演示视频链接（§0/§3）③ 系统架构图（§4.1，文字框图已就绪）④ 产品截图集（§9）⑤ 源码仓库链接（§0）
